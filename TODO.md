# Project: Actor-Orchestrator Implementation

## 🎯 Blackboard Tasks

- [ ] Install and configure `zmx` binary on host system.
- [ ] Register `actor-orchestrator` extension with `gemini extensions link`.
- [ ] Initialize dummy repo to test Supervisor -> Worker spawning loop.
- [ ] Validate Playwright MCP server connectivity for Worker UI testing.

## 🔗 Pull Requests
(Workers will append PR links here)
